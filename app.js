const isLogin = (user) => {
  if (username =="ali" || username =="mohammad"){
    return true;
  }
  else return false;
};

const register = (user) => {
  return true;
};

const getMe = (user) => {
  return "user";
};

const addCourse = (course) => {
  return true;
};

const test = () => false;
